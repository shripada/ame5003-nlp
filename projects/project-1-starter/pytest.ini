[pytest]
testpaths = tests
# The corpus tests are switched off by default, because they build the real
# 10,788-document index and take a few minutes. Run them with `pytest -m slow`,
# or run everything with `pytest -m ""`.
addopts = -q --strict-markers -m "not slow"
markers =
    slow: builds the full Reuters index
